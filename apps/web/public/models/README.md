# Procedural runtime models

This version intentionally contains no binary model files. Original, editable procedural R3F geometry is loaded through the dynamically imported `UniverseScene` module. See `assets/source/README.md` for provenance, geometry sources and performance budgets. This follows the approved Task 6 alternative where Blender tooling is unavailable.
